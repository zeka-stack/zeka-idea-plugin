请根据以下 Git 提交记录生成项目 Release Log。

**重要前提：**
- 所有提供的提交记录都属于**同一个版本**
- 只需要输出**一个版本号**（格式：x.y.z）
- 所有提交记录都归类到这个单一版本下
- 如果提交记录中包含版本号信息，请使用该版本号；否则请根据提交记录推断合适的版本号

要求：
1. 输出格式必须使用 HTML 格式，严格按照以下结构：
   - 使用 `<h3>` 标签表示版本号（格式：x.y.z，**只输出一个版本号**）
   - 使用 `<ul>` 和 `<li>` 标签表示列表
   - 每个条目使用 `<b>` 标签加粗类型前缀，格式：`<b>[表情符号] [类型]</b>：描述内容`
   - **重要：同一个版本号必须出现两次，严格按照以下顺序：**
     * 第一次：中文版本（`<h3>版本号</h3>` + 中文 `<ul><li>` 列表，包含所有提交记录）
     * 第二次：英文版本（`<h3>版本号</h3>` + 英文 `<ul><li>` 列表，包含所有提交记录）
   - 必须完整输出中文版本后，再输出英文版本，不能混合
   - **禁止为不同提交或不同类别生成多个版本号，所有内容都属于同一个版本**

2. 类型前缀映射规则：
   - `feat:` / `✨` → `<b>✨ 新增</b>` / `<b>✨ Added</b>`
   - `fix:` / `🐛` → `<b>🐛 修复</b>` / `<b>🐛 Fixed</b>`
   - `refactor:` / `♻️` → `<b>♻️ 重构</b>` / `<b>♻️ Refactored</b>`
   - `docs:` / `📚` → `<b>📚 文档</b>` / `<b>📚 Documentation</b>`
   - `perf:` / `⚡` → `<b>⚡ 优化</b>` / `<b>⚡ Optimized</b>`
   - `test:` / `✅` → `<b>✅ 测试</b>` / `<b>✅ Tests</b>`
   - `build:` / `📦` → `<b>📦 构建</b>` / `<b>📦 Build</b>`
   - `chore:` / `🔧` → `<b>🔧 优化</b>` / `<b>🔧 Optimized</b>` 或 `<b>🔧 Chore</b>`
   - `ci:` / `🔄` → `<b>🔄 CI</b>` / `<b>🔄 CI</b>`
   - 其他 → `<b>⚙️ 其他</b>` / `<b>⚙️ Miscellaneous</b>`

3. 内容处理规则：
   - 每个条目应重写为简洁、易读的描述，删除提交信息中的类型前缀（如 feat:、fix: 等）
   - 删除无意义或琐碎的提交（例如"更新代码"、"合并分支"、"chore: update"等）
   - **合并相似提交记录：** 如果存在内容相似或重复的提交记录（例如多个提交都是修复同一个问题、更新同一个功能、优化同一个模块等），必须合并为一条更新记录，避免重复
   - **合并相关提交记录：** 如果存在逻辑相关的提交记录（例如：实现某个功能及其测试、修复某个问题及其文档更新、重构某个模块及其相关优化等），应该合并为一条更新记录，使变更日志更加简洁和易读
   - 合并后的描述应该概括所有相关提交的核心内容，使用更通用的表述方式
   - 保持句子简短、客观和技术性
   - 中文版本使用中文描述，英文版本使用英文描述

4. 输出结构示例（注意：同一个版本号先中文后英文）：
```html
<h3>1.8.0</h3>
<ul>
    <li><b>✨ 新增</b>：新增反馈面板, 可在插件配置页面直接提交反馈</li>
    <li><b>🔧 优化</b>：优化 Agent Server 逻辑，完善自启动与 Jar 版本更新检测功能</li>
</ul>
<h3>1.8.0</h3>
<ul>
    <li><b>✨ Added</b>: New feedback panel, can submit feedback directly on the plugin configuration page</li>
    <li><b>🔧 Optimized</b>: Optimized Agent Server logic, improved auto-start and Jar version update detection functionality</li>
</ul>
```
   **关键要求：**
   - 第一个 `<h3>1.8.0</h3>` 及其 `<ul>` 内容必须是中文
   - 第二个 `<h3>1.8.0</h3>` 及其 `<ul>` 内容必须是英文
   - 两个版本号之间不能插入其他内容

5. 特殊说明：
   - 如果某个版本有特别注意事项，可以在该版本的英文版本列表后添加（先中文注意事项，后英文注意事项）：
     ```html
     <p><b>⚠️ 特别注意事项：</b></p>
     <ol>
         <li>支持最低版本 2024.2</li>
     </ol>
     <p><b>⚠️ Special Notice:</b></p>
     <ol>
         <li>Minimum supported version: 2024.2</li>
     </ol>
     ```
   - 只输出 HTML 内容，不要添加任何解释或注释
   - 如果某个类别没有提交记录，则不要输出该类别

6. **输出顺序总结（重要）：**
   **所有提交记录都属于同一个版本，必须严格按照以下顺序输出：**
   1. `<h3>版本号</h3>`（中文版本标题，**只有一个版本号**）
   2. `<ul>`（中文列表开始）
   3. `<li>` 条目（中文内容，**包含所有提交记录**）
   4. `</ul>`（中文列表结束）
   5. `<h3>版本号</h3>`（英文版本标题，**同一个版本号**）
   6. `<ul>`（英文列表开始）
   7. `<li>` 条目（英文内容，**包含所有提交记录**）
   8. `</ul>`（英文列表结束）
   9. 如果有特别注意事项，在英文列表后添加（先中文，后英文）
   
   **关键约束：**
   - 只输出一个版本号，不要为不同提交生成多个版本
   - 所有提交记录都归类到这一个版本下
   - 版本号在中文和英文部分使用相同的值

提交记录:

{commits}
