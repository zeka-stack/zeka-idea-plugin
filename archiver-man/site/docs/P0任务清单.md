# Archive Man P0 任务清单

面向版本 `0.3.x` 的基础只读浏览能力，以下任务为硬性交付项。状态说明：

- `TODO`：尚未开始
- `DOING`：开发中
- `DONE`：已完成

## 1. 框架与抽象

| 任务         | 描述                                                                         | 状态   |
|------------|----------------------------------------------------------------------------|------|
| `P0-FR-01` | 定义 `ArchiveFormatProvider` 接口，约束判定、根节点解析与节点构造等能力                           | DONE |
| `P0-FR-02` | 实现 `ZipArchiveFormatProvider`（覆盖 Zip/Jar），并提供 `ArchiveFormatRegistry` 统一管理 | DONE |
| `P0-FR-03` | 为 Project View 增加设置开关（暂使用临时开关常量，后续迁移到正式设置页）                                | DONE |

## 2. Project View 能力

| 任务         | 描述                                               | 状态    |
|------------|--------------------------------------------------|-------|
| `P0-PV-01` | 重写 `ArchiveTreeStructureProvider`，接入 Provider 体系 | DONE  |
| `P0-PV-02` | 节点降级与错误日志：耗时操作 try-catch，失败时回退原节点并记录             | DONE  |
| `P0-PV-03` | 基础 UI：节点图标、可导航行为优化                               | DOING |

## 3. 嵌套归档缓存

| 任务         | 描述                                              | 状态    |
|------------|-------------------------------------------------|-------|
| `P0-NA-01` | 实现 `NestedArchiveCacheService`，负责哈希命名、目录准备、引用计数 | DOING |
| `P0-NA-02` | 支持直接流式复制 & 回退到输入流复制；提供清理 API                    | TODO  |
| `P0-NA-03` | TreeStructureProvider 集成嵌套缓存（`path` 含 `!/` 时触发） | DONE  |

## 4. 稳定性与测试

| 任务         | 描述                                        | 状态   |
|------------|-------------------------------------------|------|
| `P0-QA-01` | 为 Provider/缓存新增单元测试骨架（Zip/Jar happy path） | TODO |
| `P0-QA-02` | README/用户手册更新“基础浏览”章节（完成后执行）              | TODO |

> **备注**：完成 `P0-FR-*`、`P0-PV-*`、`P0-NA-*` 三个分组后，即可宣布 P0 功能完成，再进入 P1 多格式拓展阶段。

