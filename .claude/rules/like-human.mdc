---
alwaysApply: false
globs:
  - "**/*.mdx"
  - "docs/**/*.md"
  - "**/README.md"
---

## Markdown 文档写作规范

### 核心原则：像真人一样写作，而不是 AI 生成的模板

当编写 Markdown 技术文档时，请遵循以下规则：

#### 1. 使用第一人称视角

❌ **错误示例（AI 味太浓）：**

```markdown
StringTemplate 是一个用于生成文本输出的 Java 模板引擎。本文将介绍其核心特性和使用方法。
```

✅ **正确示例：**

```markdown
最近在学习 Spring AI 的时候，看到官方文档中提到了一个很陌生的名词：StringTemplate。
作为一个写了多年 Java 的开发者，说实话以前还真没用过这个模板引擎...
```

#### 2. 加入真实的学习过程和思考

必须包含以下元素：

- **疑问和困惑**：展示最初的不理解
- **探索过程**：说明是如何逐步理解的
- **个人思考**：加入自己的理解和见解
- **实际痛点**：说明为什么需要学这个

❌ **错误示例：**

```markdown
## 核心特性

1. 严格的模型-视图分离
2. 简洁的语法
3. 自动转义
```

✅ **正确示例：**

```markdown
## 为什么不直接用字符串替换？

最开始我也觉得，提示词模板这事儿，直接 String.format() 不就行了？
比如：

```java
String prompt = String.format("请用 %s 语言解释 %s", language, topic);
```

看起来很简单对吧？但实际使用中你会遇到这些问题...

```

#### 3. 使用口语化表达

**推荐的口语化表达：**
- "说实话..."
- "我也觉得..."
- "看起来很简单对吧？"
- "带着好奇心..."
- "现在应该能理解了吧？"
- "这点特别好..."
- "这个背景很有意思..."
- "让我来简单记录一下..."

**避免使用的教条化表达：**
- ❌ "本文将介绍..."
- ❌ "下面我们来看..."
- ❌ "综上所述..."
- ❌ "总而言之..."
- ❌ "首先...其次...最后..."

#### 4. 通过实际例子引出问题

不要直接列举特性，而是通过实际遇到的问题来引出：

❌ **错误：**
```markdown
## 特性
- 支持自定义分隔符
- 防止注入攻击
```

✅ **正确：**

```markdown
### 1. 分隔符冲突

当你的提示词中包含 JSON 或代码时：

```java
String prompt = "请分析这段 JSON: {json}";
// 如果 json = "{\"name\": \"test\"}"
// 直接替换就出问题了，{} 和 {json} 分不清！
```

看到问题了吧？这就是为什么需要自定义分隔符...

```

#### 5. 承认不知道和学习过程

不要假装什么都懂，承认学习过程：

✅ **正确示例：**
```markdown
说实话，一开始看到这个配置我也是懵的，不知道这些参数都是干嘛的。
后来查了文档才发现...
```

```markdown
我之前一直以为 XXX 是这样的，后来发现其实是...
这个理解上的转变对我帮助很大。
```

#### 6. 对比和类比

使用对比和类比帮助理解：

✅ **正确示例：**

```markdown
这个设计理念很特别：一个用来生成代码的模板引擎，和我们常用的
那些用来渲染网页的模板引擎，设计理念是不一样的。
```

```markdown
| 特性      | StringTemplate         | Velocity      |
|---------|------------------------|---------------|
| 业务逻辑   | ❌ 禁止（强制分离）      | ✅ 允许        |
| 学习成本   | ⭐️ 5 分钟上手          | ⭐️⭐️ 需要学习 |
```

#### 7. 总结要有个人色彩

不要写成标准的"总结"，要有个人思考：

❌ **错误：**

```markdown
## 总结

本文介绍了 StringTemplate 的特性和使用方法，通过学习本文，
读者应该能够掌握基本用法。
```

✅ **正确：**

```markdown
## 我的理解

通过这次学习，我明白了：

1. 提示词模板不是简单的字符串替换，需要考虑分隔符冲突、转义等问题
2. StringTemplate 的设计理念很特别，强制模板单纯化，适合代码生成和提示词管理
3. Spring AI 的选择很有道理，简单、安全、易用

如果你也在用 Spring AI，现在应该知道为什么要用 {variable} 这种形式了吧？
这不是随便设计的，背后有 StringTemplate 这个精心设计的模板引擎在支撑。
```

#### 8. 代码示例要有解释和思考

不要只贴代码，要解释为什么这样写：

✅ **正确示例：**

```markdown
看起来是不是比字符串拼接清爽多了？

```java
public String promptWithTemplate(String language, String topic) {
    return chatClient
        .prompt()
        .user(u -> u
            .text("用 {language} 一句话解释 {topic}")
            .param("language", language)
            .param("topic", topic))
        .call()
        .content();
}
```

注意这里用的是 lambda 表达式，这样可以链式调用，代码可读性好很多。

```

#### 9. 避免过于正式的格式

**不要写成论文格式：**
- ❌ 摘要
- ❌ 关键词
- ❌ 引言
- ❌ 参考文献（可以有，但不要太正式）

**推荐的章节命名：**
- ✅ "前言" / "写在前面"
- ✅ "为什么需要 XXX？"
- ✅ "我的理解" / "总结"
- ✅ "实际使用" / "来看看怎么用"

#### 10. 适当使用 Emoji 和符号

让文档更生动：
- ✅ ❌ 用来表示对错
- 💡 用来表示想法
- ⚠️ 用来表示注意事项
- 🤔 用来表示疑问

但不要过度使用。

---

## 示例对比

### ❌ AI 味太浓的写法

```markdown
# XXX 技术详解

## 简介

XXX 是一个用于解决 YYY 问题的工具。本文将详细介绍其特性和使用方法。

## 核心特性

1. 特性一：描述
2. 特性二：描述
3. 特性三：描述

## 使用方法

下面我们来看如何使用该工具...

## 总结

综上所述，XXX 是一个强大的工具...
```

### ✅ 真人写作风格

```markdown
# XXX 使用笔记

## 前言

最近在做 YYY 的时候，遇到了 ZZZ 的问题。查了半天资料，
发现了这个叫 XXX 的工具。说实话，一开始看文档的时候还挺懵的，
不过用了一段时间后，觉得还挺有意思的。

今天简单记录一下我的理解。

## 为什么需要它？

你可能会想，这个问题用 AAA 方法不就行了？

我最开始也是这么想的，比如：

```code
// 简单的解决方案
```

看起来很简单对吧？但实际使用中你会遇到这些问题：

1. 问题一（具体场景）
2. 问题二（具体场景）

现在能理解为什么需要 XXX 了吧？

## 我的使用体验

...

## 总结

通过这次学习，我明白了...

```

---

## 记住

**最重要的原则：想象你在给朋友解释一个你刚学会的技术，而不是在写教科书。**

- 可以有困惑
- 可以有错误的理解
- 可以有"哦原来如此"的顿悟
- 可以有"说实话我也不太懂"的坦诚
- 可以有个人的见解和吐槽

让读者感觉是在听一个真实的开发者分享经验，而不是在阅读 AI 生成的标准文档。
