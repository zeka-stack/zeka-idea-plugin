---
description: IntelliJ IDEA 插件图标生成与优化规范
globs: [ "**/*.svg" ]
alwaysApply: true
---

## Rule: 生成 svg

### Instruction

在生成 SVG 图标时：

- 所有文字必须使用 `<path>` 元素表示，禁止使用 `<text>`、`<tspan>` 或任何依赖字体的标签。
- 输出必须是完整、可独立使用的 SVG 文件，不依赖系统字体或外部资源。
- 默认画布大小：`width="40" height="40"`，并包含 `viewBox="0 0 40 40"`。
- 图标应居中，边缘留白合理，保持视觉平衡。
- 背景可使用纯色矩形（建议带圆角 `rx=4`），除非明确要求，不使用渐变。
- 所有颜色使用十六进制或 rgba 格式，禁止引用 CSS 类或外部样式。
- 输出必须为有效 SVG（XML 头可省略）。

---

## Rule: 生成浅色主题图标

### Instruction

在生成 IntelliJ 插件浅色主题图标（`pluginIcon.svg`）时：

- 画布大小固定为 `40x40`。
- 背景颜色使用 `#4a63d4`（蓝色系），圆角半径 `rx=4`。
- 主标题与副标题使用白色 (`#ffffff`)，并以路径形式绘制。
- 所有内容应居中排列，可加入轻微分隔线提升层次感。
- 禁止使用 `<text>` 元素。

---

## Rule: 生成深色主题图标

### Instruction

在生成 IntelliJ 插件深色主题图标（`pluginIcon_dark.svg`）时：

- 画布大小固定为 `40x40`。
- 背景颜色使用 `#1e2130`（深灰色），圆角半径 `rx=4`。
- 主文字颜色使用 `#aab4ff`（浅蓝色），副标题颜色可略带透明度 `0.8`。
- 设计应在深色 UI 下保持清晰、柔和，不刺眼。
- 禁止使用 `<text>` 元素，所有文字转为路径。
- 输出必须为有效 SVG。

---

## Rule: 生成工具栏图标

### Instruction

在生成 IntelliJ 工具栏或按钮图标（通常 `16x16`）时：

- 使用简单图形或字母组合的路径，不包含文字内容。
- 画布大小固定为 `16x16`，`viewBox="0 0 16 16"`。
- 使用纯色或双色方案，确保在浅色和深色主题下均清晰。
- 禁止使用渐变、阴影或字体。
- 输出为路径形式的 SVG，居中对齐。

---

## Rule: 优化 svg

### Instruction

当用户请求优化或精简 SVG 时：

- 保留所有路径 (`<path>`) 与结构完整性。
- 删除无用属性（如 `id`、`class`、`style`）。
- 合并同色路径，减少体积。
- 确保生成结果仍为 IntelliJ 兼容 SVG。