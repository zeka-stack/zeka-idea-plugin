## 2025.11.04

- [x] 代码压缩, 减少 token 使用量

  添加控制开关, 默认关闭
  使用内置格式化功能统一格式, 然后使用正则压缩代码, 但要保证代码层级关系, 避免 AI 理解错误;
  要一个代码压缩的工具类, 输出压缩后的代码

- [x] 详细日志

  Console 控制台输出请求响应日志
  最后输出统计信息

- [ ] token 统计

  添加开关: 是否开启 Token 统计功能
  添加开关: 是否发送统计信息
  Token 计算工具类, 在发送和接收数据时计算有效 Token 数
  不同阶段解锁不同称号

## 2025.11.05

- [ ] @author 优化: 使用占位符或直接读取系统用户名
  如果 javadoc 注释中已有 @author 就不要改了(优化提示词)
- [ ] 增加非标准 javadoc 标签, 比如 @date
- [ ] @since 优化: 读取 maven pom.xml 文件中的版本号
- [ ] 任务完成后, 在控制台输出总 Token 使用量统计

## 2025.11.06

更新插件后 apiKey 丢失

## 2025.11.07

1. 提交页面添加 Javadoc 生成功能
2. 在最终插入 javadoc 之前, 先将 AI 返回的 javadoc 处理一下:
    1. 在中英文之间添加空格增加可读性;
    2. 中文标点使用英文标点符号代替;

## 2025.11.24

1. 性能模式资源竞争问题(同时写同一个文件导致添加的注释位置错误)

## 2025.12.03

1. 状态栏添加一个快速打开设置页面的选项(参考翻译插件)

## 2025.12.04

1. 实现 Kotlin 语言支持

## 2026.01.04

- [ ] 新增配置: 保存文件时自动执行 javadoc 生成(走 fix 逻辑)

## 功能增强建议

### 多语言支持

- [x] **Kotlin 语言支持**
    - 支持 Kotlin 类、方法、属性的文档生成
    - Kotlin 特定的文档标签（@param、@return 等）
    - Kotlin 协程和扩展函数的特殊处理

- [x] **多语言文档生成**
    - 支持生成英文、中文、日文等多语言文档
    - 根据项目语言设置自动选择文档语言
    - 支持同一元素生成多语言文档

### 文档质量提升

- [x] **文档质量检查**
    - 检查文档完整性（是否有缺失的 @param、@return 等）
    - 检查文档格式规范性
    - 提供文档质量评分和建议

- [ ] **文档模板库**
    - 提供多种文档风格模板（Google Style、Oracle Style 等）
    - 支持自定义文档模板
    - 支持按项目或包应用不同模板

- [ ] **文档版本管理**
    - 记录文档生成历史
    - 支持文档版本对比
    - 支持回滚到之前的文档版本

### 智能功能增强

- [ ] **代码理解增强**
    - 分析方法调用关系，生成更准确的文档
    - 识别设计模式，在文档中说明
    - 分析异常处理逻辑，完善 @throws 标签

- [ ] **上下文感知增强**
    - 分析类的整体职责，生成更符合上下文的文档
    - 识别继承关系，自动补充父类文档信息
    - 分析接口实现，生成符合接口契约的文档

### 批量处理优化

- [ ] **增量更新优化**
    - 只更新变更的代码元素的文档
    - 支持文档差异对比
    - 支持批量更新特定类型的元素

- [ ] **并行处理优化**
    - 优化性能模式的并行策略
    - 支持自定义并行度
    - 更好的进度显示和错误处理

### 用户体验优化

- [ ] **文档预览功能**
    - 生成文档前预览效果
    - 支持编辑预览内容
    - 支持多格式预览（HTML、Markdown）

- [ ] **文档导出功能**
    - 支持导出为 HTML、Markdown、PDF 格式
    - 支持批量导出项目文档
    - 支持自定义导出模板

- [ ] **文档统计面板**
    - 显示项目文档覆盖率
    - 显示文档生成统计（总数、成功率等）
    - 显示 Token 使用统计

### 高级功能

- [ ] **自定义标签支持**
    - 支持添加自定义 Javadoc 标签
    - 支持自定义标签的 AI 生成规则
    - 支持标签验证和检查

- [ ] **文档链接生成**
    - 自动生成类、方法之间的文档链接
    - 支持外部文档链接
    - 支持 API 文档链接

- [ ] **文档国际化**
    - 支持多语言文档并存
    - 根据 IDE 语言设置选择文档语言
    - 支持文档语言切换

- [ ] **Git 集成**
    - 在 Git 提交时自动生成文档
    - 支持文档变更审查
    - 支持文档变更统计

## 2025.12.09

- [x] 参考 google-java-format 插件来实现格式化单行注释
- [x] 实现多语言注释支持

## 2025.12.11

- [ ] 添加一个 class javadoc 默认模板, 避免重置时替换成固定的默认模板(用户可自行设置)

## 2025.12.13

- [ ] 添加更多可选参数, 比如流式输出(使用列表), 绑定到 AIProviderConfig, 并添加到可用服务列表中
- [ ] 为生成流程增加预览与差异视图, 支持在应用前逐条接受/跳过生成的 Javadoc
- [ ] 支持基于模板/样式配置(例如 Google/Alibaba/自定义占位符), 并允许按模块或目录级别覆盖
- [ ] 批量处理大文件或整个模块时的进度与速率限制, 防止一次性请求过多导致 AI 限流
- [ ] 监听代码结构变更(方法签名/参数变化)后标记对应 Javadoc 过期并提示重新生成
- [ ] 为 AI 响应添加质量守卫: 关键词黑名单、最大长度限制、必含标签(如 @param/@return)校验
- [ ] 记录生成历史与回滚入口, 便捷对比不同提供商或模型产出的文档质量
- [ ] 提供“仅为选中方法/类生成”快捷入口, 并在编辑器内用 intention action 触发
- [ ] 增加多提供商降级与重试策略(如 QianWen 失败自动切换到 Ollama), 并暴露超时/重试配置
- [ ] 在生成时自动检测语言环境(英文/中文等)并匹配对应模板, 可配置默认语言
- [ ] 集成静态检查: 生成后自动运行 Doclint/自定义规则校验, 将问题标记到编辑器中

## 2025.12.15

- [ ] 在类注释生成时添加类关系上下文: dev.dong4j.zeka.stack.idea.plugin.task.TaskCollector.buildGenerationContext
- [ ] 添加一个面板, 根据 token 的用量来解锁成就
- [ ] 添加一个开关: 允许上传 token 统计, 在 landing 页面展示统计信息
- [ ] 优化代码, 及时释放临时对象, 新增生命周期管理

## 2026.01.05

- [ ] 添加调用链到类和方法中作为上下文
- [ ] 将 maven 作为可选项, 没有 maven 依赖时将 version 和 since 设置为默认的 x.x.x

## 2026.01.05

- [ ] git 提交面板点击生成 javadoc 时, 错误的扫描到了非项目文件

## 2026.01.06

- [ ] 替换 git-cliff 中的 github 链接地址

```
commit_preprocessors = [
    # 将问题编号替换为链接模板，在 `changelog.postprocessors` 中更新
    #{ pattern = '\((\w+\s)?#([0-9]+)\)', replace = "([#${2}](<REPO>/issues/${2}))"},
    # 使用 https://github.com/crate-ci/typos 检查提交消息的拼写
    # 如果拼写不正确，将自动修复
    #{ pattern = '.*', replace_command = 'typos --write-changes -' },
    { pattern = '\((\w+\s)?#([0-9]+)\)', replace = "([#${2}](https://github.com/zeka-stack/zeka-idea-plugin/issues/${2}))" },
]
```
