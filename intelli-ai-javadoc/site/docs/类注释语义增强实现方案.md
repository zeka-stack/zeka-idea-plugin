# 类注释语义增强实现方案

## 一、背景

### 1.1 问题描述

当前 IntelliAI Javadoc 插件在生成类级别 Javadoc 注释时，主要依赖类本身的代码内容（字段、方法等）来推断类的职责和用途。这种方式存在以下局限性：

1. **缺乏架构上下文**：AI 无法准确判断类在系统分层架构中的位置（Controller、Service、Repository、Domain 等）
2. **缺乏使用场景信息**：无法了解类被哪些类型的组件调用（REST 控制器、定时任务、事件监听器等）
3. **缺乏依赖关系信息**：无法准确描述类的依赖关系（数据库访问、远程服务调用、事件发布等）
4. **缺乏设计意图**：无法推断类的设计意图（业务逻辑封装、基础设施隔离等）

这些问题导致生成的类注释往往：

- 描述过于泛泛，缺乏针对性
- 无法准确反映类在系统中的实际角色
- 无法体现类的设计意图和使用场景

### 1.2 解决方案概述

通过 PSI（Program Structure Interface）分析类的调用关系、依赖关系、架构位置等信息，将这些**结构级信息**转换为**语义级摘要**，作为上下文信息传递给
AI，从而显著提升类注释的准确性和针对性。

**核心思想**：

- PSI 负责**分析**（提取证据）
- Prompt 负责**解释**（语义化表达）
- AI 负责**表达**（生成注释）

## 二、功能概述

### 2.1 功能目标

为类级别的 Javadoc 生成提供丰富的语义上下文，包括：

1. **类的角色定位**：在分层架构中的位置（Layer）
2. **类的职责描述**：主要功能和业务领域（Responsibility）
3. **类的暴露范围**：内部使用还是对外暴露（Exposure）
4. **类的使用场景**：被哪些类型的组件调用（Usage Context）
5. **类的依赖关系**：依赖的外部服务和组件（Dependencies）
6. **类的设计意图**：设计模式和架构原则（Design Intent）

### 2.2 功能特点

- **语义级摘要**：不是直接 dump 调用图，而是提取高信噪比的语义信息
- **缓存机制**：基于 PsiModificationTracker 缓存分析结果，避免重复计算
- **异步处理**：PSI 分析在后台线程执行，不阻塞 EDT
- **可扩展性**：设计中间语义模型，便于后续扩展和复用

## 三、实现方案

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    DocumentationTask                        │
│                  (包含 PsiClass 元素)                        │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              ClassSemanticAnalyzer                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  1. 分析类的架构位置 (Layer)                          │  │
│  │  2. 分析类的职责 (Responsibility)                     │  │
│  │  3. 分析类的暴露范围 (Exposure)                       │  │
│  │  4. 分析类的使用场景 (Usage Context)                  │  │
│  │  5. 分析类的依赖关系 (Dependencies)                   │  │
│  │  6. 推断类的设计意图 (Design Intent)                  │  │
│  └──────────────────────────────────────────────────────┘  │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              ClassSemanticModel                             │
│  (中间语义模型，包含所有分析结果)                            │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              SemanticPromptBuilder                           │
│  (将语义模型转换为 Prompt 文本)                             │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              AIRequestComposer                              │
│  (合并到用户提示词中)                                        │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 核心数据结构

#### 3.2.1 ClassSemanticModel

```java
/**
 * 类语义模型
 * <p>
 * 用于存储通过 PSI 分析得到的类的语义信息，包括架构位置、职责、使用场景等。
 * 该模型作为 PSI 分析和 Prompt 生成之间的中间层，便于扩展和复用。
 *
 * @author zeka.stack.team
 * @version 1.0.0
 * @since 1.0.0
 */
public class ClassSemanticModel {
    /** 类在分层架构中的位置 */
    private String layer;

    /** 类的主要职责和业务领域 */
    private String responsibility;

    /** 类的暴露范围（内部使用 / 对外暴露） */
    private String exposure;

    /** 调用者类型集合（如：REST控制器、定时任务） */
    private Set<String> callerTypes = new LinkedHashSet<>();

    /** 依赖关系集合（如：UserRepository (数据库访问)） */
    private Set<String> dependencies = new LinkedHashSet<>();

    /** 副作用行为集合（如：发布领域事件） */
    private Set<String> sideEffects = new LinkedHashSet<>();

    /** 设计意图集合（如：封装业务规则） */
    private Set<String> designIntents = new LinkedHashSet<>();

    // Getters and Setters...
}
```

### 3.3 核心类设计

#### 3.3.1 ClassSemanticAnalyzer

负责通过 PSI API 分析类的各种语义信息，填充 `ClassSemanticModel`。

**主要方法**：

```java
/**
 * 分析类的语义信息
 *
 * @param psiClass 要分析的类
 * @param project  项目对象
 * @return 类的语义模型
 */
@NotNull
public ClassSemanticModel analyze(@NotNull PsiClass psiClass, @NotNull Project project) {
    ClassSemanticModel model = new ClassSemanticModel();

    // 1. 分析架构位置
    model.setLayer(resolveLayer(psiClass));

    // 2. 分析职责
    model.setResponsibility(resolveResponsibility(psiClass));

    // 3. 分析暴露范围
    model.setExposure(resolveExposure(psiClass, project));

    // 4. 分析使用场景
    analyzeUsageContext(psiClass, project, model);

    // 5. 分析依赖关系
    analyzeDependencies(psiClass, model);

    // 6. 推断设计意图
    inferDesignIntent(psiClass, model);

    return model;
}
```

#### 3.3.2 SemanticPromptBuilder

负责将 `ClassSemanticModel` 转换为 Prompt 文本。

**主要方法**：

```java
/**
 * 构建类注释的语义上下文 Prompt
 *
 * @param model 类的语义模型
 * @return Prompt 文本
 */
@NotNull
public String buildClassSemanticPrompt(@NotNull ClassSemanticModel model) {
    StringBuilder prompt = new StringBuilder();
    prompt.append("类的角色摘要：\n\n");
    prompt.append("- 架构层级：").append(model.getLayer()).append("\n");
    prompt.append("- 主要职责：").append(model.getResponsibility()).append("\n");
    prompt.append("- 暴露范围：").append(model.getExposure()).append("\n\n");

    prompt.append("使用场景：\n\n");
    if (!model.getCallerTypes().isEmpty()) {
        prompt.append("- 主要由 ").append(String.join("、", model.getCallerTypes())).append(" 调用\n");
    }

    prompt.append("\n依赖关系：\n\n");
    for (String dependency : model.getDependencies()) {
        prompt.append("- ").append(dependency).append("\n");
    }
    if (!model.getSideEffects().isEmpty()) {
        for (String sideEffect : model.getSideEffects()) {
            prompt.append("- ").append(sideEffect).append("\n");
        }
    }

    prompt.append("\n设计意图：\n\n");
    for (String intent : model.getDesignIntents()) {
        prompt.append("- ").append(intent).append("\n");
    }

    return prompt.toString();
}
```

## 四、技术细节

### 4.1 Layer（架构位置）分析

**判断优先级**：

1. 注解（最准确）
2. 包名（次准确）
3. 调用者类型（兜底）

**实现代码**：

```java
/**
 * 解析类在分层架构中的位置
 *
 * @param psiClass 要分析的类
 * @return 架构层级（控制器 / 服务层 / 仓库层 / 领域层 / 基础设施层 / 未知）
 */
@NotNull
private String resolveLayer(@NotNull PsiClass psiClass) {
    // 1. 优先检查注解
    if (AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.web.bind.annotation.RestController", 0)) {
        return "控制器";
    }
    if (AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.stereotype.Service", 0)) {
        return "服务层";
    }
    if (AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.stereotype.Repository", 0)) {
        return "仓库层";
    }
    if (AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.stereotype.Component", 0)) {
        return "组件";
    }

    // 2. 检查包名
    PsiFile containingFile = psiClass.getContainingFile();
    if (containingFile instanceof PsiJavaFile) {
        String packageName = ((PsiJavaFile) containingFile).getPackageName();
        if (packageName.contains(".controller") || packageName.contains(".web")) {
            return "控制器";
        }
        if (packageName.contains(".service") || packageName.contains(".biz")) {
            return "服务层";
        }
        if (packageName.contains(".repository") || packageName.contains(".dao")) {
            return "仓库层";
        }
        if (packageName.contains(".domain") || packageName.contains(".model")
                || packageName.contains(".entity")) {
            return "领域层";
        }
        if (packageName.contains(".infrastructure") || packageName.contains(".infra")) {
            return "基础设施层";
        }
    }

    // 3. 默认返回未知（后续可通过调用者类型推断）
    return "未知";
}
```

### 4.2 Responsibility（职责）分析

**策略**：组合类名拆词、方法名动词统计、被调用场景。

**实现代码**：

```java
/**
 * 解析类的主要职责
 *
 * @param psiClass 要分析的类
 * @return 职责描述
 */
@NotNull
private String resolveResponsibility(@NotNull PsiClass psiClass) {
    // 1. 从类名提取领域关键词
    String className = psiClass.getName();
    String domain = extractDomainFromClassName(className);

    // 2. 统计方法名中的动词
    Set<String> verbs = new LinkedHashSet<>();
    for (PsiMethod method : psiClass.getMethods()) {
        if (method.isConstructor() || method.hasModifierProperty(PsiModifier.PRIVATE)) {
            continue;
        }
        String methodName = method.getName();
        String verb = extractVerb(methodName);
        if (verb != null) {
            verbs.add(verb);
        }
    }

    // 3. 映射动词到职责描述
    String responsibility = mapVerbsToResponsibility(verbs);

    // 4. 组合领域和职责
    if (domain != null && !domain.isEmpty()) {
        return domain + " " + responsibility;
    }
    return responsibility;
}

/**
 * 从类名提取领域关键词
 */
private String extractDomainFromClassName(@NotNull String className) {
    // 移除常见后缀：Service, Controller, Repository, Manager, Handler 等
    String domain = className
        .replaceAll("Service$", "")
        .replaceAll("Controller$", "")
        .replaceAll("Repository$", "")
        .replaceAll("Manager$", "")
        .replaceAll("Handler$", "")
        .replaceAll("Impl$", "");

    // 转换为小写，便于理解
    return domain.isEmpty() ? null : domain.toLowerCase();
}

/**
 * 从方法名提取动词
 */
@Nullable
private String extractVerb(@NotNull String methodName) {
    // 常见动词模式
    if (methodName.startsWith("create") || methodName.startsWith("register")) {
        return "creation";
    }
    if (methodName.startsWith("update") || methodName.startsWith("modify")) {
        return "modification";
    }
    if (methodName.startsWith("delete") || methodName.startsWith("remove")) {
        return "deletion";
    }
    if (methodName.startsWith("find") || methodName.startsWith("get")
            || methodName.startsWith("query") || methodName.startsWith("search")) {
        return "query";
    }
    if (methodName.startsWith("validate") || methodName.startsWith("check")) {
        return "validation";
    }
    if (methodName.startsWith("publish") || methodName.startsWith("send")
            || methodName.startsWith("emit")) {
        return "event_dispatching";
    }
    return null;
}

/**
 * 映射动词到职责描述
 */
@NotNull
private String mapVerbsToResponsibility(@NotNull Set<String> verbs) {
    if (verbs.contains("creation")) {
        return "创建和管理逻辑";
    }
    if (verbs.contains("query")) {
        return "查询和检索逻辑";
    }
    if (verbs.contains("validation")) {
        return "验证逻辑";
    }
    if (verbs.contains("event_dispatching")) {
        return "事件分发逻辑";
    }
    return "业务逻辑";
}
```

### 4.3 Exposure（暴露范围）分析

**判断规则**：

- Controller 一律认为是 external
- 被其他模块引用 → external
- 否则 → internal

**实现代码**：

```java
/**
 * 解析类的暴露范围
 *
 * @param psiClass 要分析的类
 * @param project  项目对象
 * @return 暴露范围（仅内部使用 / 作为公共API暴露）
 */
@NotNull
private String resolveExposure(@NotNull PsiClass psiClass, @NotNull Project project) {
    // 1. Controller 一律认为是 external
    if (isController(psiClass)) {
        return "作为公共API暴露";
    }

    // 2. 检查是否被其他模块引用
    GlobalSearchScope projectScope = GlobalSearchScope.projectScope(project);
    Collection<PsiReference> references = ReferencesSearch.search(psiClass, projectScope).findAll();

    for (PsiReference ref : references) {
        PsiElement element = ref.getElement();
        PsiClass caller = PsiTreeUtil.getParentOfType(element, PsiClass.class);
        if (caller != null && !sameModule(caller, psiClass)) {
            return "作为公共API暴露";
        }
    }

    return "仅内部使用";
}

/**
 * 判断两个类是否在同一模块
 */
private boolean sameModule(@NotNull PsiClass class1, @NotNull PsiClass class2) {
    PsiFile file1 = class1.getContainingFile();
    PsiFile file2 = class2.getContainingFile();
    if (file1 == null || file2 == null) {
        return false;
    }

    VirtualFile vf1 = file1.getVirtualFile();
    VirtualFile vf2 = file2.getVirtualFile();
    if (vf1 == null || vf2 == null) {
        return false;
    }

    // 简单判断：同一包路径或同一模块路径
    String path1 = vf1.getPath();
    String path2 = vf2.getPath();

    // 提取模块路径（假设模块在独立的目录下）
    // 这里可以根据实际项目结构调整
    return path1.contains("/src/main/java/") && path2.contains("/src/main/java/");
}
```

### 4.4 Usage Context（使用场景）分析

**分析维度**：

1. 调用者类型（Controller、Job、Listener 等）

**说明**：

- 只分析调用者的类型（架构角色），不分析具体的业务场景
- 调用者类型信息足以帮助 AI 理解类的使用上下文
- 避免通过方法名/路径硬编码映射业务场景，因为不准确且无法完全覆盖

**实现代码**：

```java
/**
 * 分析类的使用场景
 *
 * @param psiClass 要分析的类
 * @param project  项目对象
 * @param model    语义模型（用于填充结果）
 */
private void analyzeUsageContext(@NotNull PsiClass psiClass,
                                 @NotNull Project project,
                                 @NotNull ClassSemanticModel model) {
    GlobalSearchScope projectScope = GlobalSearchScope.projectScope(project);
    Collection<PsiReference> references = ReferencesSearch.search(psiClass, projectScope).findAll();

    Set<String> callerTypes = new LinkedHashSet<>();

    for (PsiReference ref : references) {
        PsiElement element = ref.getElement();
        PsiClass caller = PsiTreeUtil.getParentOfType(element, PsiClass.class);
        if (caller == null) {
            continue;
        }

        // 识别调用者类型（架构角色）
        if (isController(caller)) {
            callerTypes.add("REST控制器");
        } else if (isJob(caller)) {
            callerTypes.add("定时任务");
        } else if (isListener(caller)) {
            callerTypes.add("事件监听器");
        } else if (isService(caller)) {
            callerTypes.add("服务组件");
        } else if (isRepository(caller)) {
            callerTypes.add("数据访问层");
        } else if (isComponent(caller)) {
            callerTypes.add("组件");
        }
    }

    model.setCallerTypes(callerTypes);
}

/**
 * 判断是否是 Controller
 */
private boolean isController(@NotNull PsiClass psiClass) {
    return AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.web.bind.annotation.RestController", 0)
            || AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.stereotype.Controller", 0);
}

/**
 * 判断是否是定时任务
 */
private boolean isJob(@NotNull PsiClass psiClass) {
    return AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.scheduling.annotation.Scheduled", 0)
            || psiClass.getName() != null && psiClass.getName().contains("Job");
}

/**
 * 判断是否是事件监听器
 */
private boolean isListener(@NotNull PsiClass psiClass) {
    return AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.context.event.EventListener", 0)
            || psiClass.getName() != null && psiClass.getName().contains("Listener");
}

/**
 * 判断是否是 Service
 */
private boolean isService(@NotNull PsiClass psiClass) {
    return AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.stereotype.Service", 0);
}

/**
 * 判断是否是 Repository
 */
private boolean isRepository(@NotNull PsiClass psiClass) {
    return AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.stereotype.Repository", 0);
}

/**
 * 判断是否是 Component
 */
private boolean isComponent(@NotNull PsiClass psiClass) {
    return AnnotationUtil.isAnnotated(psiClass,
            "org.springframework.stereotype.Component", 0);
}

### 4.5 Dependencies（依赖关系）分析

**分析维度**：
1. 注入字段（@Autowired、构造函数注入）
2. 类型语义映射（Repository → 数据库访问）

**实现代码**：

```java
/**
 * 分析类的依赖关系
 *
 * @param psiClass 要分析的类
 * @param model    语义模型（用于填充结果）
 */
private void analyzeDependencies(@NotNull PsiClass psiClass, @NotNull ClassSemanticModel model) {
    Set<String> dependencies = new LinkedHashSet<>();
    Set<String> sideEffects = new LinkedHashSet<>();

    // 1. 分析字段注入
    for (PsiField field : psiClass.getFields()) {
        if (AnnotationUtil.isAnnotated(field,
                "org.springframework.beans.factory.annotation.Autowired", 0)
                || hasConstructorInjection(field)) {

            PsiType type = field.getType();
            String typeName = type.getPresentableText();

            // 类型语义映射
            String semanticDescription = mapTypeToSemantic(typeName);
            if (semanticDescription != null) {
                dependencies.add(semanticDescription);
            }
        }
    }

    // 2. 分析方法调用中的副作用
    for (PsiMethod method : psiClass.getMethods()) {
        if (method.isConstructor()) {
            continue;
        }

        // 检查方法体中是否有事件发布、IO 操作等
        PsiCodeBlock body = method.getBody();
        if (body != null) {
            if (containsMethodCall(body, "publish", "send", "emit")) {
                sideEffects.add("发布领域事件");
            }
            if (containsMethodCall(body, "save", "persist", "flush")) {
                sideEffects.add("执行数据库操作");
            }
        }
    }

    model.setDependencies(dependencies);
    model.setSideEffects(sideEffects);
}

/**
 * 将类型映射到语义描述
 */
@Nullable
private String mapTypeToSemantic(@NotNull String typeName) {
    // Repository 模式
    if (typeName.endsWith("Repository") || typeName.endsWith("DAO")) {
        return typeName + " (数据库访问)";
    }

    // Client / Feign 模式
    if (typeName.endsWith("Client") || typeName.endsWith("Feign")) {
        return typeName + " (远程服务)";
    }

    // Publisher / Producer 模式
    if (typeName.endsWith("Publisher") || typeName.endsWith("Producer")) {
        return typeName + " (事件发布)";
    }

    // Template 模式（如 RestTemplate, JdbcTemplate）
    if (typeName.endsWith("Template")) {
        return typeName + " (模板操作)";
    }

    // Mapper 模式
    if (typeName.endsWith("Mapper")) {
        return typeName + " (数据映射)";
    }

    return null;
}

/**
 * 检查方法体中是否包含指定方法调用
 */
private boolean containsMethodCall(@NotNull PsiCodeBlock body,
                                   @NotNull String... methodNames) {
    // 使用 PsiRecursiveElementVisitor 遍历方法体
    AtomicBoolean found = new AtomicBoolean(false);
    body.accept(new JavaRecursiveElementVisitor() {
        @Override
        public void visitMethodCallExpression(@NotNull PsiMethodCallExpression expression) {
            String calledMethodName = expression.getMethodExpression().getReferenceName();
            if (calledMethodName != null) {
                for (String methodName : methodNames) {
                    if (calledMethodName.toLowerCase().contains(methodName.toLowerCase())) {
                        found.set(true);
                        return;
                    }
                }
            }
            super.visitMethodCallExpression(expression);
        }
    });
    return found.get();
}
```

### 4.6 Design Intent（设计意图）推断

**推断规则**（基于特征推断）：

| 特征              | 设计意图     |
|-----------------|----------|
| 无 IO / Client   | 避免基础设施关注 |
| 方法名偏业务          | 封装业务规则   |
| 无 static util   | 面向对象设计   |
| 无 Controller 注解 | 不负责请求处理  |

**实现代码**：

```java
/**
 * 推断类的设计意图
 *
 * @param psiClass 要分析的类
 * @param model    语义模型（用于填充结果）
 */
private void inferDesignIntent(@NotNull PsiClass psiClass, @NotNull ClassSemanticModel model) {
    Set<String> intents = new LinkedHashSet<>();

    // 1. 检查是否有基础设施依赖
    boolean hasInfrastructureDependency = false;
    for (PsiField field : psiClass.getFields()) {
        PsiType type = field.getType();
        String typeName = type.getPresentableText();
        if (typeName.contains("Repository") || typeName.contains("Client")
                || typeName.contains("Template")) {
            hasInfrastructureDependency = true;
            break;
        }
    }
    if (!hasInfrastructureDependency) {
        intents.add("避免基础设施关注");
    }

    // 2. 检查方法名是否偏业务
    boolean hasBusinessMethods = false;
    for (PsiMethod method : psiClass.getMethods()) {
        if (method.isConstructor()) {
            continue;
        }
        String methodName = method.getName().toLowerCase();
        if (methodName.contains("create") || methodName.contains("validate")
                || methodName.contains("calculate") || methodName.contains("process")) {
            hasBusinessMethods = true;
            break;
        }
    }
    if (hasBusinessMethods) {
        intents.add("封装业务规则");
    }

    // 3. 检查是否是工具类
    boolean isUtilClass = psiClass.hasModifierProperty(PsiModifier.FINAL)
            && hasOnlyStaticMethods(psiClass);
    if (!isUtilClass) {
        intents.add("面向对象设计");
    }

    // 4. 检查是否是 Controller
    if (!isController(psiClass)) {
        intents.add("不负责请求处理");
    }

    model.setDesignIntents(intents);
}
```

### 4.7 缓存机制

**实现思路**：

- 基于 `PsiModificationTracker` 跟踪类结构变化
- 只在类结构变化时重新分析
- 使用 `CachedValue` 或自定义缓存实现

**实现代码**：

```java
/**
 * 带缓存的语义分析器
 */
public class CachedClassSemanticAnalyzer {
    private final Map<PsiClass, CachedValue<ClassSemanticModel>> cache = new ConcurrentHashMap<>();

    @NotNull
    public ClassSemanticModel analyze(@NotNull PsiClass psiClass, @NotNull Project project) {
        return cache.computeIfAbsent(psiClass, key -> {
            CachedValueProvider<ClassSemanticModel> provider = () -> {
                ClassSemanticAnalyzer analyzer = new ClassSemanticAnalyzer();
                return CachedValueProvider.Result.create(
                    analyzer.analyze(key, project),
                    PsiModificationTracker.MODIFICATION_COUNT.get(project)
                );
            };
            return CachedValuesManager.getManager(project)
                .createCachedValue(provider, false);
        }).getValue();
    }
}
```

### 4.8 异步处理

**实现思路**：

- PSI 分析在后台线程执行
- 使用 `ReadAction.nonBlocking()` 确保线程安全
- 分析完成后通过 `invokeLater()` 更新 UI

**实现代码**：

```java
/**
 * 异步分析类的语义信息
 *
 * @param psiClass 要分析的类
 * @param project  项目对象
 * @param callback 分析完成后的回调
 */
public void analyzeAsync(@NotNull PsiClass psiClass,
                        @NotNull Project project,
                        @NotNull Consumer<ClassSemanticModel> callback) {
    ReadAction.nonBlocking(() -> {
        ClassSemanticAnalyzer analyzer = new ClassSemanticAnalyzer();
        return analyzer.analyze(psiClass, project);
    })
    .inSmartMode(project)
    .expireWith(project)
    .finishOnUiThread(ModalityState.any(), callback)
    .submit(AppExecutorUtil.getAppExecutorService());
}
```

## 五、涉及的文件和类

### 5.1 新增文件

1. **ClassSemanticModel.java**
    - 路径：`src/main/java/dev/dong4j/zeka/stack/idea/javadoc/semantic/ClassSemanticModel.java`
    - 职责：存储类的语义信息

2. **ClassSemanticAnalyzer.java**
    - 路径：`src/main/java/dev/dong4j/zeka/stack/idea/javadoc/semantic/ClassSemanticAnalyzer.java`
    - 职责：通过 PSI 分析类的语义信息

3. **SemanticPromptBuilder.java**
    - 路径：`src/main/java/dev/dong4j/zeka/stack/idea/javadoc/semantic/SemanticPromptBuilder.java`
    - 职责：将语义模型转换为 Prompt 文本

4. **CachedClassSemanticAnalyzer.java**（可选）
    - 路径：`src/main/java/dev/dong4j/zeka/stack/idea/javadoc/semantic/CachedClassSemanticAnalyzer.java`
    - 职责：带缓存的语义分析器

### 5.2 修改文件

1. **AIRequestComposer.java**
    - 修改位置：`buildUserPrompt()` 方法
    - 修改内容：在构建类注释提示词时，先分析类的语义信息，然后将语义上下文合并到提示词中

2. **DocumentationTask.java**
    - 修改位置：可能需要添加语义模型字段（可选）

3. **TaskCollector.java**
    - 修改位置：收集类任务时，可能需要触发语义分析（可选）

### 5.3 依赖的 IntelliJ Platform API

- `com.intellij.psi.PsiClass`
- `com.intellij.psi.PsiMethod`
- `com.intellij.psi.PsiField`
- `com.intellij.psi.PsiReference`
- `com.intellij.psi.search.ReferencesSearch`
- `com.intellij.psi.util.PsiTreeUtil`
- `com.intellij.psi.util.AnnotationUtil`
- `com.intellij.openapi.application.ReadAction`
- `com.intellij.openapi.util.CachedValue`
- `com.intellij.psi.util.CachedValuesManager`
- `com.intellij.psi.PsiModificationTracker`

## 六、提示词模板修改说明

### 6.1 系统提示词修改

在系统提示词中添加角色定位，提升生成质量：

```java
/**
 * 获取增强后的系统提示词模板
 */
@NotNull
public static String getEnhancedSystemPromptTemplate() {
    return """
        You are acting as a senior Java architect writing documentation for other developers.
        Your task is to generate accurate, professional Javadoc comments that clearly describe
        the purpose, responsibilities, and usage of Java classes.

        When generating class-level documentation, you should:
        - Focus on the class's role in the system architecture
        - Describe its responsibilities and business domain
        - Explain its usage context and dependencies
        - Reflect its design intent and architectural principles

        Use concise, professional JavaDoc style.
        Write in ${language}.
        """;
}
```

### 6.2 类注释提示词模板修改

在类注释提示词模板中，添加语义上下文的占位符：

```java
/**
 * 获取增强后的类注释提示词模板
 */
@NotNull
public static String getEnhancedClassPromptTemplate() {
    return """
        请为以下类/接口/枚举/对象生成类级别的文档注释（${language}）。
        请自动识别代码语言（Java 或 Kotlin），如果是 Java 代码生成 Javadoc 格式，如果是 Kotlin 代码生成 KDoc 格式。

        # 重要说明
        - 下面的代码可能已经包含旧的文档注释，请忽略或改进它
        - 只返回类/接口/枚举/对象级别的文档注释，不要返回方法/函数、字段/属性等其他元素的注释
        - 不要返回代码本身，只返回注释
        - 不要使用任何 markdown 代码块标记（如 ```java 或 ```kotlin）

        # 语义上下文（重要参考信息）
        %s

        # 格式要求
        1. **必须使用${language}编写注释内容**，这是强制要求，不能使用其他语言
        2. 必须包含完整的文档注释格式，包括开始标记 /** 和结束标记 */
        3. 注释要准确描述类/接口/枚举/对象的职责、主要功能和使用场景
        4. **必须参考上面的语义上下文信息**，确保注释准确反映类在系统中的实际角色
        5. 如果语义上下文中提到了类的使用场景，请在注释中体现
        6. 如果语义上下文中提到了类的依赖关系，请在注释中说明
        7. 如果语义上下文中提到了类的设计意图，请在注释中体现
        8. Java: 如果是工具类，需要说明主要提供的功能
        9. Kotlin: 如果是工具对象（object），需要说明主要提供的功能；如果是数据类（data class），需要说明数据类的用途和主要属性
        10. 如果是接口，需要说明接口的用途和实现要求
        11. 如果是枚举，需要说明枚举的用途和各个值的含义
        12. 如果有特殊的设计模式，需要说明
        13. **代码格式规则（重要）**：如果注释中需要包含代码片段，必须使用以下格式：
           <pre>{@code
           代码片段
           }</pre>
           注意：代码片段必须放在 {@code} 标签内，并且外层使用 <pre> 标签包裹
        14. URL 格式规则: 仅当原始代码、已有注释或语义上下文中真实存在 URL 时才允许保留并按 <a href="URL">URL</a> 格式输出，严禁生成任何示例、占位或虚构的 URL
           注意：URL 必须使用 <a href="URL">URL</a> 格式，href 和显示文本都使用完整的 URL
        15. 添加 @author、@version、@email、@date、@since 标签且保存顺序
           - 如果已存在 @author 且添加了作者信息则直接使用, 否则使用 ${author} 作为作者
           - 如果已存在 @version 则保存不变, 否则使用 1.0.0 作为版本号
           - 如果已存在 @email 则保持不变, 否则使用 ${email} 作为邮箱
           - 如果已存在 @date ,需要格式化为 yyyy.mm.dd, 否则使用 ${date} 作为时间戳
           - 如果已存在 @since 则保存不变, 否则使用 ${since} 作为版本号

        # 示例说明
        **重要：以下示例仅用于展示格式，实际输出必须使用${language}编写注释内容，并参考语义上下文信息。**

        %s
        """;
}
```

**注意**：模板中使用 `%s` 作为语义上下文的占位符，需要在 `AIRequestComposer` 中填充。

### 6.3 AIRequestComposer 修改

修改 `buildUserPrompt()` 方法，在构建类注释提示词时添加语义上下文：

```java
/**
 * 根据任务类型和设置构建用户提示模板
 */
private static String buildUserPrompt(@NotNull SettingsState settings, @NotNull DocumentationTask task) {
    // ... 现有的 FIX 模式处理逻辑 ...

    // 否则使用正常的提示词模板
    String template = switch (task.getType()) {
        case CLASS, INTERFACE, ENUM -> {
            // 1. 解析类模板
            String classTemplate = resolveClassTemplate(task, settings.classPromptTemplate,
                    SettingsState.getDefaultClassPromptTemplate());

            // 2. 如果是类类型，分析语义信息
            if (task.getElement() instanceof PsiClass psiClass) {
                Project project = psiClass.getProject();

                // 3. 异步分析语义信息（这里简化处理，实际应该异步）
                ClassSemanticAnalyzer analyzer = new ClassSemanticAnalyzer();
                ClassSemanticModel semanticModel = analyzer.analyze(psiClass, project);

                // 4. 构建语义上下文 Prompt
                SemanticPromptBuilder promptBuilder = new SemanticPromptBuilder();
                String semanticContext = promptBuilder.buildClassSemanticPrompt(semanticModel);

                // 5. 合并到模板中
                String codeWithContext = mergeContextAndCode(task.getContext(), task.getCode());
                yield String.format(classTemplate, semanticContext, codeWithContext);
            }

            // 6. 如果不是类类型，使用原有逻辑
            String codeWithContext = mergeContextAndCode(task.getContext(), task.getCode());
            yield String.format(classTemplate, "", codeWithContext);
        }
        case FIELD -> {
            String template1 = resolveTemplate(settings.fieldPromptTemplate,
                    SettingsState.getDefaultFieldPromptTemplate());
            String codeWithContext = mergeContextAndCode(task.getContext(), task.getCode());
            yield String.format(template1, codeWithContext);
        }
        // ... 其他类型处理 ...
    };

    return template;
}
```

## 七、实现步骤

### 7.1 第一阶段：核心数据结构

1. 创建 `ClassSemanticModel.java`
2. 实现基本的 getter/setter 方法
3. 添加必要的工具方法

### 7.2 第二阶段：PSI 分析器

1. 创建 `ClassSemanticAnalyzer.java`
2. 实现 `resolveLayer()` 方法
3. 实现 `resolveResponsibility()` 方法
4. 实现 `resolveExposure()` 方法
5. 实现 `analyzeUsageContext()` 方法
6. 实现 `analyzeDependencies()` 方法
7. 实现 `inferDesignIntent()` 方法
8. 实现主方法 `analyze()`

### 7.3 第三阶段：Prompt 构建器

1. 创建 `SemanticPromptBuilder.java`
2. 实现 `buildClassSemanticPrompt()` 方法
3. 添加格式化辅助方法

### 7.4 第四阶段：集成到现有流程

1. 修改 `AIRequestComposer.java`
2. 在 `buildUserPrompt()` 中集成语义分析
3. 修改类注释提示词模板，添加语义上下文占位符

### 7.5 第五阶段：优化和缓存

1. 实现 `CachedClassSemanticAnalyzer.java`（可选）
2. 添加异步分析支持
3. 性能优化和测试

## 八、测试计划

### 8.1 单元测试

1. **ClassSemanticAnalyzerTest**
    - 测试 `resolveLayer()` 方法（注解、包名、兜底）
    - 测试 `resolveResponsibility()` 方法（类名、方法名）
    - 测试 `resolveExposure()` 方法（Controller、跨模块引用）
    - 测试 `analyzeUsageContext()` 方法（调用者类型）
    - 测试 `analyzeDependencies()` 方法（注入字段、类型映射）
    - 测试 `inferDesignIntent()` 方法（设计意图推断）

2. **SemanticPromptBuilderTest**
    - 测试 `buildClassSemanticPrompt()` 方法
    - 测试各种边界情况（空集合、null 值等）

### 8.2 集成测试

1. **端到端测试**
    - 选择一个真实的类（如 UserService）
    - 执行完整的语义分析流程
    - 验证生成的 Prompt 是否正确
    - 验证生成的 Javadoc 是否准确

2. **性能测试**
    - 测试大量类的分析性能
    - 测试缓存机制的有效性
    - 测试异步处理的正确性

### 8.3 测试用例示例

```java
/**
 * 测试用例：UserService 类
 */
public class UserServiceSemanticAnalysisTest {
    @Test
    public void testAnalyzeUserService() {
        // 1. 准备测试数据
        PsiClass userService = ...; // 获取 UserService 类

        // 2. 执行分析
        ClassSemanticAnalyzer analyzer = new ClassSemanticAnalyzer();
        ClassSemanticModel model = analyzer.analyze(userService, project);

        // 3. 验证结果
        assertEquals("服务层", model.getLayer());
        assertTrue(model.getResponsibility().contains("user"));
        assertEquals("仅内部使用", model.getExposure());
        assertTrue(model.getCallerTypes().contains("REST控制器"));
        assertTrue(model.getDependencies().stream()
            .anyMatch(d -> d.contains("UserRepository")));
        assertTrue(model.getDesignIntents().contains("封装业务规则"));
    }
}
```

## 九、注意事项

### 9.1 线程安全

- PSI 读取必须使用 `ReadAction`
- UI 更新必须使用 `invokeLater()`
- 缓存操作需要考虑并发安全

### 9.2 性能考虑

- PSI 分析可能较耗时，必须异步执行
- 使用缓存避免重复分析
- 只在必要时进行分析（如生成类注释时）

### 9.3 边界情况处理

- 处理匿名类、内部类等特殊情况
- 处理无法确定层级的类（返回 "未知"）
- 处理没有调用者的类（空集合）
- 处理没有依赖的类（空集合）

### 9.4 可扩展性

- 设计中间语义模型，便于后续扩展
- 支持自定义类型映射规则
- 支持自定义类型映射规则
- 支持扩展调用者类型识别（通过添加新的 `isXxx()` 方法）

## 十、预期效果

### 10.1 提升准确性

- 类注释能够准确反映类在系统中的实际角色
- 类注释能够准确描述类的使用场景
- 类注释能够准确说明类的依赖关系

### 10.2 提升针对性

- 不同层级的类（控制器、服务层、仓库层）生成不同风格的注释
- 不同使用场景的类生成不同侧重点的注释

### 10.3 提升专业性

- 类注释更符合 Java 架构师的专业水准
- 类注释更符合企业级开发规范

## 十一、后续优化方向

1. **支持更多架构模式**：如 DDD、CQRS、微服务等
2. **支持自定义规则**：允许用户配置类型映射、场景识别等规则
3. **支持多语言**：扩展支持 Kotlin、Scala 等语言
4. **支持方法级语义分析**：将语义分析扩展到方法级别
5. **支持字段级语义分析**：将语义分析扩展到字段级别

---

**文档版本**：1.0.0
**创建日期**：2025.01.XX
**作者**：zeka.stack.team
