/**
 * 并行任务处理包
 * <p>
 * 本包实现了高性能的多线程并行文档生成处理系统，采用"消消乐式"并行处理算法，
 * 支持多服务商并行处理、任务重试、错误恢复和负载均衡等功能。
 *
 * <h2>核心算法：消消乐式并行处理</h2>
 * <p>
 * 该算法将文档生成任务组织成类似消消乐游戏的结构：
 * <ul>
 *   <li><b>文件 = 列</b>：每个文件被视为一个独立的列</li>
 *   <li><b>任务 = 块</b>：文件中的每个元素（类、方法、字段）是一个块</li>
 *   <li><b>线程 = 乒乓球</b>：多个工作线程并发从队列中获取任务</li>
 *   <li><b>顺序约束</b>：同一文件的任务必须按顺序处理（一个块消除后才能处理下一个）</li>
 *   <li><b>并行处理</b>：不同文件的任务可以并行处理</li>
 * </ul>
 *
 * <h3>算法优势</h3>
 * <ul>
 *   <li><b>最大化并行度</b>：不同文件的任务可以同时处理，充分利用多核 CPU</li>
 *   <li><b>保证文件内顺序</b>：同一文件的任务串行处理，避免文件写入冲突</li>
 *   <li><b>负载均衡</b>：使用轮询策略分发任务，确保各线程负载均衡</li>
 *   <li><b>动态调整</b>：根据任务数量和服务商数量动态计算线程数</li>
 * </ul>
 *
 * <h2>架构组件</h2>
 *
 * <h3>1. ParallelTaskExecutor（并行任务执行器）</h3>
 * <p>
 * 核心协调器，负责：
 * <ul>
 *   <li>初始化任务分发器和服务商管理器</li>
 *   <li>根据任务数量和服务商数量动态计算线程数</li>
 *   <li>为每个服务商创建独立的线程池</li>
 *   <li>启动工作线程并等待所有任务完成</li>
 *   <li>收集和输出统计信息</li>
 * </ul>
 *
 * <h3>2. TaskDispatcher（任务分发器）</h3>
 * <p>
 * 任务分发中心，负责：
 * <ul>
 *   <li>按文件路径分组任务，为每个文件创建独立的队列</li>
 *   <li>管理文件队列和重试队列</li>
 *   <li>使用轮询策略从文件队列中分发任务（负载均衡）</li>
 *   <li>当所有文件队列为空时，从重试队列获取任务</li>
 *   <li>提供统一的任务获取接口</li>
 * </ul>
 *
 * <h3>3. FileTaskQueue（文件任务队列）</h3>
 * <p>
 * 单文件任务队列，负责：
 * <ul>
 *   <li>管理单个文件的所有任务</li>
 *   <li>使用 {@link java.util.concurrent.locks.ReentrantLock} 保证同一文件的任务串行处理</li>
 *   <li>提供线程安全的任务添加和获取接口</li>
 *   <li>确保同一文件的任务按顺序处理，避免文件写入冲突</li>
 * </ul>
 *
 * <h3>4. ParallelTaskWorker（并行任务工作线程）</h3>
 * <p>
 * 工作线程，负责：
 * <ul>
 *   <li>从任务分发器获取任务</li>
 *   <li>执行文档生成（调用 AI 服务）</li>
 *   <li>处理超时（默认 10 秒）</li>
 *   <li>处理错误和重试逻辑</li>
 *   <li>更新进度和统计信息</li>
 *   <li>处理 429 限流错误（标记服务商不可用）</li>
 * </ul>
 *
 * <h3>5. ProviderManager（服务商管理器）</h3>
 * <p>
 * 服务商状态管理，负责：
 * <ul>
 *   <li>管理所有服务商的状态（可用、限流、错误）</li>
 *   <li>管理服务商的线程池和执行器</li>
 *   <li>当服务商出现 429 错误时，标记为限流状态并销毁所有线程</li>
 *   <li>提供服务商可用性检查接口</li>
 * </ul>
 *
 * <h3>6. RetryQueue（重试队列）</h3>
 * <p>
 * 重试任务管理，负责：
 * <ul>
 *   <li>管理所有需要重试的任务</li>
 *   <li>当任务执行失败（非 429 错误）时，将任务加入重试队列</li>
 *   <li>支持最大重试次数（默认 3 次）</li>
 *   <li>重试队列中的任务可以被任何可用的线程获取</li>
 * </ul>
 *
 * <h3>7. RetryableTask（可重试任务）</h3>
 * <p>
 * 重试任务封装，负责：
 * <ul>
 *   <li>封装原始任务和重试信息（重试次数、最后错误）</li>
 *   <li>跟踪重试次数，防止无限重试</li>
 *   <li>记录最后一次错误信息</li>
 * </ul>
 *
 * <h2>工作流程</h2>
 *
 * <h3>初始化阶段</h3>
 * <ol>
 *   <li>创建任务分发器，按文件路径分组任务</li>
 *   <li>为每个文件创建独立的文件队列</li>
 *   <li>创建服务商管理器，注册所有服务商</li>
 *   <li>根据任务数量和服务商数量计算线程数</li>
 *   <li>为每个服务商创建独立的线程池</li>
 * </ol>
 *
 * <h3>执行阶段</h3>
 * <ol>
 *   <li>为每个服务商启动多个工作线程</li>
 *   <li>工作线程循环执行：
 *     <ul>
 *       <li>从任务分发器获取任务（优先从文件队列，其次从重试队列）</li>
 *       <li>获取文件队列锁（如果是文件队列任务）</li>
 *       <li>执行任务（调用 AI 服务生成文档）</li>
 *       <li>处理结果：
 *         <ul>
 *           <li>成功：更新统计信息，释放锁</li>
 *           <li>失败：根据错误类型处理（429 错误标记服务商不可用，其他错误加入重试队列）</li>
 *           <li>跳过：更新统计信息，释放锁</li>
 *         </ul>
 *       </li>
 *       <li>释放文件队列锁</li>
 *     </ul>
 *   </li>
 *   <li>当所有队列为空且所有线程空闲时，处理完成</li>
 * </ol>
 *
 * <h3>错误处理</h3>
 * <ul>
 *   <li><b>429 限流错误</b>：
 *     <ul>
 *       <li>标记服务商为限流状态</li>
 *       <li>销毁该服务商的所有线程</li>
 *       <li>任务标记为失败，不加入重试队列</li>
 *     </ul>
 *   </li>
 *   <li><b>其他错误</b>：
 *     <ul>
 *       <li>创建重试任务并加入重试队列</li>
 *       <li>如果重试次数超过最大限制（3 次），标记为最终失败</li>
 *       <li>否则，任务会被重新处理</li>
 *     </ul>
 *   </li>
 *   <li><b>超时</b>：
 *     <ul>
 *       <li>任务执行超过 10 秒时，取消任务并加入重试队列</li>
 *     </ul>
 *   </li>
 * </ul>
 *
 * <h2>线程数计算策略</h2>
 * <p>
 * 根据任务数量和服务商数量动态计算线程数：
 * <ul>
 *   <li><b>任务数 ≤ 10</b>：每个服务商 1 个线程</li>
 *   <li><b>10 < 任务数 ≤ 50</b>：每个服务商 2 个线程</li>
 *   <li><b>任务数 > 50</b>：每个服务商 3-4 个线程（根据任务数动态计算）</li>
 *   <li><b>限制</b>：总线程数不超过任务数</li>
 * </li>
 * </ul>
 *
 * <h2>性能优化</h2>
 * <ul>
 *   <li><b>文件级锁</b>：只锁定单个文件的队列，不同文件可以并行处理</li>
 *   <li><b>非阻塞锁</b>：使用 tryLock() 避免线程阻塞，提高并发度</li>
 *   <li><b>轮询分发</b>：使用轮询策略确保负载均衡</li>
 *   <li><b>动态线程池</b>：根据任务数量动态调整线程数</li>
 *   <li><b>多服务商并行</b>：支持多个 AI 服务商同时处理任务</li>
 * </ul>
 *
 * <h2>线程安全</h2>
 * <ul>
 *   <li>使用 {@link java.util.concurrent.ConcurrentHashMap} 保证并发安全</li>
 *   <li>使用 {@link java.util.concurrent.ConcurrentLinkedQueue} 实现线程安全的队列</li>
 *   <li>使用 {@link java.util.concurrent.locks.ReentrantLock} 保证文件队列的串行处理</li>
 *   <li>使用原子类（AtomicInteger）更新统计信息</li>
 *   <li>进度更新在 EDT（Event Dispatch Thread）中执行</li>
 * </ul>
 *
 * <h2>使用示例</h2>
 * <pre>{@code
 * // 创建并行任务执行器
 * ParallelTaskExecutor executor = new ParallelTaskExecutor(
 *     project, aiService, providerSettings, settings,
 *     indicator, documentationInserter, progressManager
 * );
 *
 * // 执行并行任务处理
 * boolean success = executor.execute(tasks, providers);
 * }</pre>
 *
 * <h2>注意事项</h2>
 * <ul>
 *   <li>并行模式适用于任务数量大于 10 的场景</li>
 *   <li>需要至少一个可用的 AI 服务商</li>
 *   <li>同一文件的任务会串行处理，确保文件写入安全</li>
 *   <li>429 错误会导致服务商的所有线程被销毁</li>
 *   <li>重试任务的最大重试次数为 3 次</li>
 * </ul>
 *
 * @author zeka.stack.team
 * @version 1.0.0
 * @email mailto:zeka.stack@gmail.com
 * @date 2025.12.01
 * @since 1.0.0
 */

package dev.dong4j.zeka.stack.idea.plugin.task.parallel;

